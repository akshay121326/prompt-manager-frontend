# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
on:
  push:
    branches:
      - main
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Create .env.production
        run: |
          cat <<'EOF' > .env.production
          ${{ secrets.PRODUCTION_ENV }}
          EOF
      - name: Build Verification (Debug)
        run: |
          echo "--- DEBUG INFO ---"
          ls -l .env.production
          echo "Byte count: $(wc -c < .env.production)"
          echo "Detected Keys:"
          grep -o '^VITE_[^=]*' .env.production || echo "None"
          echo "------------------"
      - name: Build and Deploy
        run: |
          # Load the env file into the build process shell
          export $(grep -v '^#' .env.production | xargs)
          npm ci
          npm run build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_PROMPT_PRO_3DF27 }}
          channelId: live
          projectId: prompt-pro-3df27
